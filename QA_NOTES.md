# QA 메모 — 코드 플로우 추적 점검 (2026-08-24)

퍼블리싱 단계 수정(크래시 방지·성능·UX·연동 대비) 이후, 스플래시→로그인/가입→탭 5개→상세·시트까지 전체 플로우를 코드 레벨로 추적한 결과. 백엔드는 아직 미연동 상태 기준.

## ⚡ 최적화·사용자 경험 개선 점검 (2026-08-25)

1. ~~**세션 만료와 앱 재시작의 상태가 일관되지 않음**~~ → **수정 완료 (2026-08-25)**
   - 스플래시는 토큰의 존재만 확인하고, 리프레시 실패 시 인터셉터가 토큰만 지워 전역 세션과 화면이 로그인 상태로 남을 수 있음.
   - 로그인 사용자 요약도 재시작 뒤 복원되지 않아 인사말이 일반값으로 바뀜.
   - 토큰·사용자 요약을 메모리 캐시와 secure storage에 함께 저장하고, 리프레시 실패 시 전역 세션을 종료하도록 수정. 스플래시는 토큰 조회를 로고 노출과 병렬 실행해 사용자 정보를 함께 복원.
2. ~~**온보딩 라우트에 직접 진입할 수 있음**~~ → **수정 완료 (2026-08-25)**
   - 회원가입 인증을 완료한 사용자만 접근해야 하지만, 비로그인 딥링크도 허용됨.
   - 회원가입 완료 시에만 설정되는 온보딩 접근 상태를 추가하고, 직접 진입은 회원가입 또는 탐색 화면으로 리다이렉트하도록 수정.
3. ~~**데이터 로딩 실패 후 사용자가 다시 시도할 방법이 없음**~~ → **수정 완료 (2026-08-25)**
   - 탐색·자료방·캘린더·대화 내역이 오류 문구만 보여 줌.
   - 네 화면의 오류 상태에 provider를 다시 로드하는 `다시 시도` 버튼을 추가.
4. ~~**검색어 입력과 스플래시 초기화가 불필요하게 느림**~~ → **수정 완료 (2026-08-25)**
   - 검색은 매 글자마다 상태를 갱신하고, 토큰 조회는 2초 스플래시가 끝난 뒤 시작됨.
   - 검색어를 300ms 디바운스하고, 토큰·사용자 요약 조회는 스플래시 노출과 병렬 실행하도록 수정. 캘린더 선택일 목록도 provider에서 파생·캐시.
5. ~~**프로필 미구현 메뉴의 상태가 불명확함**~~ → **수정 완료 (2026-08-25)**
   - 탭 후에만 준비 중 스낵바가 보여 기능 사용 가능 여부를 미리 알기 어려움.
   - 미구현 메뉴는 탭을 비활성화하고 `준비 중` 상태를 화면에 명시.

## 🔴 버그로 보이는 것

1. ~~**이메일 인증 결과(`verified`·`verificationToken`)를 검증·사용하지 않음**~~ → **수정 완료 (2026-08-24)**
   - `lib/pages/auth/sign_up/view/email_verification_page.dart` `_submit`: `verifyEmailCode` 응답의 `verified`가 false여도 그대로 `signUp`을 호출하고, 모델에 "회원가입 요청 시 함께 제출해야 하는" `verificationToken`(`lib/pages/auth/model/email_verification.dart` 27~28행)이 있는데 `signUp` 요청에 포함하지 않음.
   - 서버 연동 시: 잘못된 코드로도 가입 시도가 나가고, 서버가 토큰을 요구하면 가입이 항상 실패함.
   - `verified`가 false이거나 토큰이 비어 있으면 가입을 중단하고, 성공 시 `verificationToken`을 가입 요청에 포함하도록 수정.

2. ~~**로그인 요청 진행 중 키보드 "완료"로 실패 전에 `/search` 이동 가능**~~ → **수정 완료 (2026-08-24)**
   - 비밀번호 필드의 `onSubmitted`가 `isSubmitting`을 확인하지 않아, 첫 요청이 끝나기 전 완료 키를 누르면 `login()`이 조용히 반환되어 성공으로 오인 → 탐색 화면으로 이동했음.
   - `lib/pages/auth/login/view/login_page.dart`: `isSubmitting` 중에는 완료 키 제출을 무시하도록 수정.

3. ~~**캘린더 기본(더미) 일정을 수정 없이 저장할 수 없음**~~ → **수정 완료 (2026-08-24)**
   - 더미 일정 4건이 모두 `startAt == endAt`으로 생성되는데(`lib/pages/calendar/provider/calendar_repository.dart` 11~40행), 일정 폼의 새 검증 규칙이 "종료가 시작보다 늦어야 함"이라(`schedule_form_sheet.dart` `_submit`) 기본 일정을 열고 제목만 고쳐 저장하려 하면 시간도 함께 바꿔야 함.
   - 더미 데이터의 종료 시각을 시작보다 늦게 주거나, 실서버 데이터 규격을 확인해 맞출 것.
   - 기본 일정 4건 모두 종료 시각을 시작 시각보다 1시간 뒤로 보정.

4. ~~**로그아웃해도 앱 내 상태 대부분이 남아 있음**~~ → **수정 완료 (2026-08-24)**
   - 로그아웃은 토큰 삭제와 로그인 폼 리셋만 수행(`lib/pages/my/view/my_page.dart` `_logout`). 로드맵 대화, 검색어·카테고리, 자료방 목록, 캘린더 일정, 회원가입 폼 상태는 non-autoDispose provider라 그대로 유지됨.
   - 다른 계정으로 재로그인하는 시나리오에서 이전 사용자의 대화·자료가 그대로 보임. 로그아웃 시 관련 provider invalidate 필요.
   - 로그인·가입 폼, 검색, 자료방, 캘린더, 대화 상태와 in-memory 저장소를 로그아웃 시 초기화하도록 수정.

## 🟡 확인은 됐지만 의도된 placeholder로 보이는 것

5. ~~**스플래시가 저장된 토큰이 있어도 항상 `/login`으로 이동**~~ → **수정 완료 (2026-08-24)** — 스플래시가 저장된 액세스 토큰을 확인해 탐색 또는 로그인 화면으로 이동하도록 수정.
6. ~~**셸 라우트(`/profile`, `/calendar` 등)에 인증 가드 없음**~~ → **수정 완료 (2026-08-24)** — 세션 상태를 라우터 redirect에 연결해 미로그인 딥링크를 로그인 화면으로 보냄.
7. ~~**온보딩 "다음" 버튼 무동작 + 도달 불가 화면**~~ → **수정 완료 (2026-08-24)** — 이메일 인증 가입 완료 후 온보딩으로 이동하고, 분야 선택 후 다음 버튼으로 로그인 화면에 진입하도록 수정.
8. ~~**사용자 정보 고정값**~~ → **수정 완료 (2026-08-24)** — 탐색 인사말은 로그인 응답의 닉네임을 사용하고, 로드맵·프로필의 개인 더미 값을 제거.

## 🟢 사소한 UI 디테일

9. ~~**로드맵 탭 루트에 back 화살표가 노출되지만 눌러도 무반응**~~ → **수정 완료 (2026-08-24)** — 탭 루트의 `RoadmapChatPage`에서 back 버튼을 숨기도록 수정.
10. ~~**딥링크로 상세/잘못된 주소에 바로 진입하면 back 버튼이 무반응**~~ → **수정 완료 (2026-08-24)** — 이전 화면이 없으면 탐색 화면으로 이동하는 fallback을 추가.
11. ~~**로드맵 추천 일정 날짜가 7월로 고정**~~ → **수정 완료 (2026-08-24)** — 추천 일정 더미 날짜를 현재 날짜 기준으로 생성.

## 검증 노트

- 자동 리뷰가 지적한 "Authorization 헤더가 `******` 문자열로 전송됨"은 **오탐**. 실제 파일은 `Bearer` + 토큰 보간이 맞음(보안 마스킹 표시 문제).
- `flutter analyze` 0건, `flutter test` 41개 전부 통과 상태에서 점검함.

---

# QA 메모 — iPhone 17 시뮬레이터 전체 화면 점검 (2026-08-18)

iOS 26.1 / iPhone 17 시뮬레이터에서 `flutter run`으로 앱을 빌드해 로그인부터 탭 5개(탐색/로드맵/자료방/캘린더/프로필)까지 직접 조작하며 확인한 결과.

## 🔴 버그로 보이는 것

1. ~~**마이페이지(프로필) 하위 메뉴가 전부 동작하지 않음 — 특히 "로그아웃"**~~ → **로그아웃은 수정 완료 (2026-08-18)**
   - `lib/pages/my/view/my_page.dart`: "로그아웃"에 `ConfirmDialog`(공용 확인 다이얼로그) → 확인 시 `context.go('/login')`으로 로그인 화면으로 보내도록 구현. 시뮬레이터에서 확인 다이얼로그 표출 → 로그인 화면 복귀까지 재확인함.
   - "희망 분야 수정", "알림 설정", "이용약관"은 준비 중 안내를 표시하도록 보완됨.
   - **사이드 노트**: 로그아웃/회원가입 완료 후 `/login`으로 돌아오면, 입력창은 비어 있는데 "로그인" 버튼이 잠깐 활성화 색으로 보임 — `loginViewModelProvider`가 전역 상태라 이전 입력값이 리셋되지 않아서 생기는 시각적 디테일. 실제로 눌러보면 `Form` 자체 검증은 정상적으로 "이메일을 입력해주세요" 등을 띄우고 잘못 로그인되지는 않아 기능적으로 위험하진 않음.

2. ~~**프로필 수정 시트의 "비밀번호 변경" 링크가 빈 콜백**~~ → **수정 완료 (2026-08-24)**
   - `lib/pages/my/widget/custom_bottom_sheet.dart` 148행: `onTap: () {}` — 탭해도 아무 반응 없음. 아직 미구현 상태로 보임.
   - 무반응 대신 준비 중 안내 스낵바를 표시하도록 수정.

## 🟡 확인은 됐지만 의도된 placeholder로 보이는 것

3. ~~**캘린더 탭 첫 진입 시 선택 날짜가 항상 "이번 달 15일"로 고정**~~ → **수정 완료 (2026-08-24)**
   - `lib/pages/calendar/view/calendar_page.dart` 32~35행, 코드 주석에 "더미 일정이 이번 달 15일에 몰려 있어, 처음 열었을 때부터 바로 보이게 한다"고 명시됨.
   - 실제 오늘 날짜(예: 8/18)가 아니라 15일 일정이 보임. 더미 데이터용 임시 처리로 보이나, 실 서비스 붙일 때 `DateTime.now().day`로 바꿔야 함.
   - 캘린더 최초 선택 날짜를 현재 날짜로 변경.

4. ~~**캘린더에서 등록된 일정을 탭해도 상세/수정/삭제가 안 됨**~~ → **수정 완료 (2026-08-19)**
   - 저장소(`calendar_repository.dart`)·뷰모델(`calendar_view_model.dart`)에 `updateEvent`/`removeEvent`(`edit`/`remove`)를 추가하고, `ScheduleFormSheet`가 기존 일정으로 초기화되는 수정 모드를 지원하도록 확장. `CalendarEventTile`을 탭하면 상세/수정 시트가 열리고, ⋮ 메뉴로 수정·삭제(확인 다이얼로그 포함) 가능.
   - 구현 중 실제 버그 하나 발견·수정: 타일 전체를 감싸는 큰 `GestureDetector` 안에 ⋮ 버튼의 작은 `GestureDetector`를 중첩시켰더니 ⋮를 눌러도 메뉴 대신 타일의 onTap(상세)이 반응해 "삭제"를 선택할 수 없었음. ⋮ 버튼을 상세 탭 영역과 형제(sibling)로 분리해 해결. (`lib/pages/calendar/widget/calendar_event_tile.dart`)
   - 시뮬레이터에서 일정 추가 → 제목 수정 → 삭제(확인 다이얼로그 → 실제 목록에서 제거)까지 전부 라이브로 확인함.

5. ~~**로드맵 "대화 내역" 목록이 실제 대화 내용과 연결되어 있지 않음**~~ → **수정 완료 (2026-08-24)**
   - `lib/pages/roadmap/chat_history/view/chat_history_page.dart` 64~68행: 카드를 탭하면 `certificateName`만 넘겨서 `/roadmap/chat/:certificateName`으로 이동함.
   - 목록엔 "3개월 학습 로드맵 수립...", "24개 메시지" 같은 요약이 자격증별로 여러 개 있는데(예: 정보처리기사 7/13, 7/9 두 건), 실제로 열어보면 그 요약과 무관하게 항상 똑같은 초기 인사말만 있는 새 대화가 열림. 같은 자격증의 서로 다른 대화 기록을 구분해서 볼 수 없음.
   - 대화 내역 ID를 경로와 상태 키에 전달해 세션별 대화 내용을 분리하도록 수정.

## 🟢 사소한 UI 디테일

4. ~~로그인 성공 시 뜨는 초록 스낵바("로그인되었습니다")가 탐색 화면 하단 리스트 카드 마지막 줄과 살짝 겹침.~~ → **수정 완료 (2026-08-24)** — 로그인 성공 메시지는 화면 전환 전에 표시하지 않도록 수정.

## ✅ 정상 동작 확인된 것

- 로그인/회원가입 폼 유효성 검사(이메일 형식, 비밀번호 8자+영문/숫자 조합, 에러 메시지·스낵바 정상 표출)
- 탐색 탭 검색창 필터링 + 카테고리 칩 + "검색 결과가 없어요" 빈 상태
- 자격증 상세 페이지(응시료/합격률/과목별 진도율 등)
- 로드맵 챗봇 mock 대화 흐름, "캘린더에 추가" 모달(체크박스/전체선택) → 캘린더에 실제 반영됨
- 로드맵 화면 "…" 버튼 → 대화 내역 목록(검색 포함) 정상 이동
- 캘린더 월간/주간 뷰 전환, 날짜 선택, "+" 새 일정 추가 폼(제목/기간/시간/알림/연결 자격증/메모) 저장까지 정상
- 자료방 목록/카테고리 필터/상세 페이지, ⋮ 메뉴의 "수정"(폼 정상) · "삭제"(확인 다이얼로그 → 취소/삭제 정상)
- 프로필 수정 시트 자체(이름/직무/희망분야 편집 및 저장)는 정상 동작 — 칩 목록도 스크롤로 끝까지 접근 가능(처음엔 시트가 안 스크롤되는 것처럼 보였는데, 재확인 결과 정상 스크롤됨)

## 참고 (코드 관찰, 우선순위 낮음)

- `lib/core/router/app_router.dart`에 `/onboarding` 라우트(`OnboardingPage`)가 정의돼 있지만, 스플래시는 항상 `/login`으로만 이동하고 앱 어디에서도 `/onboarding`으로 push하는 곳이 없음. 현재는 도달 불가능한 화면.

## 참고 (앱 버그 아님, 테스트 환경 이슈)

- 시뮬레이터 하드웨어 키보드가 한글(2벌식) 입력 소스로 연결되어 있어서 자동 입력 시 영문이 한글 자모로 깨져 들어갔음. `AppleKeyboards`를 `en_US`만 남기고 앱을 재시작해서 해결. 실제 사용자 키보드 입력과는 무관.
